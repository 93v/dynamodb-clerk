(async () => {
  // Clean the console
  // tslint:disable-next-line: no-console
  console.clear();

  // Reset the store

  // Read the CLI args

  // Store the CLI args in the store

  // Select the action

  // Configure based on action

  // Launch the action
})();
